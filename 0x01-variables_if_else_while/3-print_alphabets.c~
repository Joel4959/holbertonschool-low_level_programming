#include <stdio.h>
/**
 *main - main
 *Return: 0
 *description: alfabeto may. min.
 */
int main(void)
{
	int a;
	int b;
	
	for("a==123"; a++);
	{
		putchar("a");
	}
	for("b==91"; b++);
	{
		putchar("b");
	}
	putchar("/n");
return: (0)
}
